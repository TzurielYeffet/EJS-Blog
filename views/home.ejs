
<%- include("parcials/header"); -%>

<h1>Home</h1>
<p><%= homeParagraph %></p>

<% posts.forEach(function(element){ %>
  <% console.log(element); %>
<% });%>

<% posts.forEach(function(element){ %>
    <h1> <%=  element.title %> </h1>
    <p> <%=  element.content.toString().substring(0,100)+ "..." %>
    <a href="/<%=element.title%>">Read More</a>
  </p>
<% }); %>

<%- include("parcials/footer"); -%>
